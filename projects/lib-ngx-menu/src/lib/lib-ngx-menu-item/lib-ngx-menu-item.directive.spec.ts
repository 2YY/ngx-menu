import { LibNgxMenuItemDirective } from './lib-ngx-menu-item.directive';

describe('LibNgxMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new LibNgxMenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
